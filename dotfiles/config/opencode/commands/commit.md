---
description: Stage, branch if needed, and commit current changes. Does not push.
agent: shipper
---

Run commit mode. Ticket ID (if provided): $ARGUMENTS

If no ticket ID, try to derive from the branch name. If that fails, ask the user.
